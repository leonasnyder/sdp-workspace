const crypto = require("crypto"); const RURL = process.env.UPSTASH_REDIS_REST_URL || process.env.KV_REST_API_URL; const RTOK = process.env.UPSTASH_REDIS_REST_TOKEN || process.env.KV_REST_API_TOKEN; async function rcmd(cmd){ const r = await fetch(RURL, { method:"POST", headers:{ Authorization:"Bearer "+RTOK, "Content-Type":"application/json" }, body: JSON.stringify(cmd) }); const j = await r.json(); return j.result; } function sig(s){ return crypto.createHmac("sha256", process.env.ADMIN_SECRET).update(s).digest("hex").slice(0,32); } module.exports = async (req, res) => { try { if (req.method !== "POST") return res.status(405).json({ ok:false }); const b = req.body || {}; const em = String(b.email || "").trim().toLowerCase(); const nm = String(b.name || "").trim().slice(0, 80); if (!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(em)) return res.status(400).json({ ok:false, error:"valid email required" }); const ex = await rcmd(["GET", "user:"+em]); if (ex) { const u = JSON.parse(ex); if (u.status === "approved") return res.json({ ok:true, message:"You're already approved - use the access link you were sent, or ask your facilitator to re-send it." }); if (u.status === "pending") return res.json({ ok:true, message:"Your request is already pending approval." }); } await rcmd(["SET", "user:"+em, JSON.stringify({ name:nm, email:em, status:"pending", requestedAt:new Date().toISOString() })]); await rcmd(["SADD", "users", em]); const host = "https://" + req.headers.host; const s = sig(em); const html = "<p><strong>" + (nm || "(no name)") + " &lt;" + em + "&gt;</strong> is requesting access to the SDP Workspace.</p>" + "<p><a href=\"" + host + "/api/approve?email=" + encodeURIComponent(em) + "&sig=" + s + "&action=approve\">APPROVE</a> &nbsp;|&nbsp; " + "<a href=\"" + host + "/api/approve?email=" + encodeURIComponent(em) + "&sig=" + s + "&action=deny\">DENY</a></p>" + "<p>Manage everyone: <a href=\"" + host + "/api/admin?sig=" + sig("admin") + "\">admin panel</a></p>"; await fetch("https://api.resend.com/emails", { method:"POST", headers:{ Authorization:"Bearer "+process.env.RESEND_API_KEY, "Content-Type":"application/json" }, body: JSON.stringify({ from:"SDP Workspace <onboarding@resend.dev>", to:[process.env.ADMIN_EMAIL], subject:"SDP Workspace access request: "+em, html }) }); res.json({ ok:true, message:"Request sent! You'll receive your access link once your facilitator approves it." }); } catch(e) { res.status(500).json({ ok:false, error:String(e.message || e) }); } };
